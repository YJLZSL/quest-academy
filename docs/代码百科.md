# 灵犀学院 Code Wiki

> 本文档为「灵犀学院（Lingxi Academy）」项目的结构化代码百科，涵盖项目整体架构、主要模块职责、关键类与函数说明、依赖关系以及项目运行方式等关键信息。
>
> - **项目定位**：开源、非商业化的引导式 AI 学习应用，以苏格拉底式对话为核心教学方式
> - **目标用户**：K12 至大学阶段的 AI 编程与 AI 应用方向学习者
> - **支持平台**：Android + Windows（不含 iOS / Linux / macOS，v0.4.0 已彻底移除 macOS）
> - **技术栈**：Flutter 3.44.4 / Dart 3.12.2 / Riverpod 2.5 / Drift 2.18 / GoRouter 14.2
> - **当前版本**：v0.4.0（双端专注版）
> - **文档最后更新**：2026-07-24（v0.4.0 同步：补充 update 模块与 `LearnerProfiles` / `LearningEvents` 表）

---

## 目录

- [一、项目概览](#一项目概览)
- [二、整体架构](#二整体架构)
- [三、目录结构](#三目录结构)
- [四、核心层（core）模块](#四核心层core模块)
- [五、数据层（data）模块](#五数据层data模块)
- [六、功能层（features）模块](#六功能层features模块)
- [七、共享层（shared）模块](#七共享层shared模块)
- [八、关键业务流程](#八关键业务流程)
- [九、依赖关系](#九依赖关系)
- [十、项目运行方式](#十项目运行方式)
- [十一、安全红线与约束](#十一安全红线与约束)
- [十二、测试体系](#十二测试体系)

---

## 一、项目概览

### 1.1 项目理念

灵犀学院是一款开源的引导式 AI 学习应用，核心理念包含三点：

1. **非商业化** —— 项目以 MIT 协议开源，不内置任何付费墙或广告
2. **用户自备 API** —— 应用本身不分发 AI 服务，用户需自行配置 API Key（OpenAI / Claude / Gemini / Ollama），API Key 仅本地加密存储，**永不上传**
3. **苏格拉底式引导** —— AI 导师以提问引导思考而非直接给出答案，培养独立思考与问题拆解能力

### 1.2 核心功能特性

| 功能 | 说明 |
|------|------|
| 🤔 引导式学习 | 苏格拉底式问答，AI 不给答案给引导 |
| 💬 自由对话 | 支持 OpenAI / Claude / Gemini / Ollama 四类 Provider |
| 📚 学习路径 L0-L4 | 结构化知识点卡片 + 测验 + 苏格拉底对话 |
| 🦏 吉祥物小犀 | 6 状态机情绪联动，点击交互，连点 5 次触发彩蛋 |
| 🔥 Streak 连续打卡 | 连续学习天数追踪 + 成就徽章系统 |
| 🔍 分级探索 | 简化 / 深入 / 图示三按钮，按需调整 AI 回复详略 |
| 🔒 数据安全 | API Key 硬件级加密存储，日志自动脱敏 |
| 🖥️ 双端支持 | Android + Windows 跨平台 |

### 1.3 学习路径层级

| 级别 | 定位 | 内容方向 |
|------|------|----------|
| L0 | 启蒙 | AI 基础概念与工具入门 |
| L1 | 基础 | 编程语言与提示词工程基础 |
| L2 | 进阶 | 机器学习与数据处理 |
| L3 | 实践 | 深度学习与模型训练 |
| L4 | 应用 | AI 项目实战与部署 |

每个知识点遵循闭环：**知识卡片学习 → 测验检验 → 苏格拉底式对话深化**，完成即解锁下一节点。

---

## 二、整体架构

### 2.1 分层架构

灵犀学院采用**严格分层架构**，依赖方向单向向下：

```
┌─────────────────────────────────┐
│         UI Layer (features/)     │   页面、组件、交互逻辑
├─────────────────────────────────┤
│     State Management (Riverpod)  │   Provider / StateNotifier
├─────────────────────────────────┤
│      Business Logic (Services)   │   成就服务、连续学习、数据导出
├─────────────────────────────────┤
│    Data Access (Repositories)    │   每表一个 Repository，封装 CRUD
├─────────────────────────────────┤
│      Storage (Drift + Secure)    │   SQLite 数据库 + SecureStorage
└─────────────────────────────────┘
```

### 2.2 各层职责

| 层 | 目录 | 职责 | 依赖方向 |
|----|------|------|----------|
| **UI 层** | `lib/features/` | 页面 Widget、业务编排、交互响应 | 依赖 State / Data / Shared |
| **状态层** | Riverpod Providers | 跨组件状态共享、依赖注入 | 依赖 Data 层 |
| **业务层** | `lib/features/*/services/` | 成就解锁、Streak 计算、数据导出 | 依赖 Data 层 |
| **数据层** | `lib/data/repositories/` | 表 CRUD、数据模型转换 | 依赖 Storage 层 |
| **存储层** | `lib/data/db/` + `lib/data/services/` | Drift SQLite + flutter_secure_storage | 只依赖第三方库 |

### 2.3 分层原则

1. **依赖方向单向向下**：上层可依赖下层，下层不可反向依赖上层
2. **core 层无业务逻辑**：`lib/core/` 只含主题、路由、常量、动画曲线等基础设施
3. **shared 层不依赖 feature**：`lib/shared/` 是跨 feature 复用的纯 UI 组件与工具
4. **业务编排集中在 features**：Controller（StateNotifier）负责调用 Repository / Service 完成业务流程

### 2.4 状态管理策略

项目使用 `flutter_riverpod` 2.5.x，所有跨组件状态必须通过 Riverpod 管理：

| Provider 类型 | 适用场景 | 项目示例 |
|--------------|----------|----------|
| `Provider` | 无状态依赖/服务/单例 | `databaseProvider`、`secureStorageServiceProvider` |
| `StateProvider` | 简单可变状态 | `themeModeProvider`、`localeProvider`、`socraticModeProvider` |
| `FutureProvider` | 异步一次性数据 | `currentAiProviderProvider`、`promptManagerProvider` |
| `StateNotifierProvider` | 复杂状态机 | `chatControllerProvider`、`mascotControllerProvider` |

> **使用约定**：
> - Repository / Service / 数据库连接等单例 Provider **不加** autoDispose
> - `ref.watch` 在 `build` 内订阅状态变化触发重建
> - `ref.read` 在回调 / 事件处理 / Controller 方法内一次性读取

---

## 三、目录结构

```
lingxi-academy/
├── lib/
│   ├── main.dart                     # 应用入口：初始化 SharedPreferences → ProviderScope
│   ├── app.dart                      # LingxiApp：MaterialApp.router 根 Widget
│   ├── core/                         # 核心层（无业务逻辑）
│   │   ├── constants/                #   常量（app_constants.dart）
│   │   ├── motion/                   #   动画曲线（spring_motion.dart, page_transitions.dart）
│   │   ├── providers/                #   全局 Provider（app_providers.dart）
│   │   ├── router/                   #   路由（app_router.dart, route_names.dart）
│   │   └── theme/                    #   主题（app_theme.dart, lingxi_colors.dart 等）
│   ├── data/                         # 数据层
│   │   ├── db/                       #   Drift 数据库
│   │   │   ├── database.dart         #     表定义 + LingxiDatabase（schemaVersion=3）
│   │   │   ├── connection.dart       #     跨端连接配置
│   │   │   ├── secure_database.dart  #     加密数据库（预留）
│   │   │   └── database.g.dart       #     代码生成文件
│   │   ├── models/                   #   纯数据模型
│   │   │   ├── provider_config.dart  #     AI Provider 配置
│   │   │   └── course_content.dart   #     课程内容层级模型
│   │   ├── providers/                #   数据层 Provider 注册
│   │   │   ├── db_providers.dart     #     Repository Provider 注册
│   │   │   ├── storage_providers.dart#     SecureStorage Provider 注册
│   │   │   └── course_providers.dart #     课程数据 Provider
│   │   ├── repositories/             #   仓库（每张表一个 Repository）
│   │   └── services/                 #   数据服务（secure_storage_service.dart）
│   ├── features/                     # 功能层（按业务模块组织）
│   │   ├── ai/                       #   AI Provider 抽象与实现、SSE、安全日志
│   │   ├── chat/                     #   对话列表与对话页
│   │   ├── help/                     #   帮助中心
│   │   ├── home/                     #   首页（含 empty_states / daily_plan 等子目录）
│   │   ├── learning/                 #   学习路径与课时（含 widgets 子目录）
│   │   ├── mascot/                   #   吉祥物小犀（controller/state/painter/widget/overlay/rive）
│   │   ├── notes/                    #   笔记列表与编辑
│   │   ├── onboarding/               #   引导与 API 配置向导
│   │   ├── progress/                 #   进度统计、成就服务、连续学习、间隔重复
│   │   ├── recommendation/           #   推荐服务
│   │   └── settings/                 #   设置、API 设置、数据导出、Provider 编辑
│   └── shared/                       # 共享层：跨 feature 复用组件与工具
│       ├── utils/                    #   工具（responsive.dart, misconception_parser.dart）
│       └── widgets/                  #   通用组件（lingxi_card, lingxi_button, lingxi_app_bar 等）
├── assets/                           # 静态资源
│   ├── images/                       #   图片资源
│   ├── courses/                      #   课程 JSON 内容（l0_python_basics.json 等）
│   ├── rive/                         #   Rive 动画资源
│   └── prompts/                      #   系统提示词（socratic_*.md, direct_answer_prompt.md）
├── docs/                             # 项目文档
├── test/                             # 测试
├── .github/workflows/                # CI/CD 配置（ci.yml, release.yml）
├── android/ windows/                 # 各平台原生工程
├── pubspec.yaml                      # 依赖配置
├── AGENTS.md                         # AI 协作者规范
├── CONTRIBUTING.md                   # 贡献指南
└── README.md                         # 项目说明
```

---

## 四、核心层（core）模块

`lib/core/` 是与业务无关的基础设施层，**不含业务逻辑**。

### 4.1 constants/app_constants.dart

纯常量定义文件，被多处引用。

| 常量 | 值 | 用途 |
|------|----|----|
| `kAppName` | `'灵犀学院'` | 应用名称 |
| `kAppVersion` | `'0.2.0'` | 应用版本 |
| `kDefaultPageSize` | `20` | 列表分页大小 |
| `kStreakMinDailyProgress` | `1` | Streak 计数最小日进度阈值 |
| `kQuizPassThreshold` | `0.8` | 测验通过正确率阈值（80%） |

### 4.2 providers/app_providers.dart

全局 Provider 与 应用配置入口。

#### 关键 Provider

| Provider | 类型 | 职责 |
|---------|------|------|
| `sharedPreferencesProvider` | `Provider<SharedPreferences>` | 必须在 `main()` 中 `overrideWithValue` 注入 |
| `appConfigProvider` | `Provider<AppConfig>` | 读取初始配置快照（主题/语言/苏格拉底/引导） |
| `themeModeProvider` | `StateProvider<ThemeMode>` | 主题模式（system/light/dark） |
| `localeProvider` | `StateProvider<Locale>` | 语言（zh-CN / en-US） |
| `socraticModeProvider` | `StateNotifierProvider<SocraticModeNotifier, bool>` | 苏格拉底引导模式开关，切换时持久化 |
| `onboardingCompletedProvider` | `StateProvider<bool>` | 引导完成标志 |

#### AppConfig 数据类

```dart
class AppConfig {
  final ThemeMode themeMode;
  final Locale locale;
  final bool socraticMode;
  final bool onboardingCompleted;
}
```

SharedPreferences 键映射：
- `theme_mode`：int (0=system, 1=light, 2=dark)
- `locale`：String ('zh' | 'en')
- `socratic_mode`：bool
- `onboarding_completed`：bool

### 4.3 router/route_names.dart

路由名称与路径常量类 `RouteNames`，统一管理避免硬编码。

```dart
class RouteNames {
  // 路由名称
  static const String onboarding = 'onboarding';
  static const String apiSetup = 'apiSetup';
  static const String home = 'home';
  static const String learning = 'learning';
  static const String lesson = 'lesson';
  static const String chatList = 'chatList';
  static const String chat = 'chat';
  static const String notes = 'notes';
  static const String noteEditor = 'noteEditor';
  static const String settings = 'settings';
  static const String settingsApi = 'settingsApi';
  static const String achievements = 'achievements';
  static const String statistics = 'statistics';
  static const String help = 'help';

  // 路由路径
  static const String homePath = '/home';
  static const String learningPath = '/learning';
  static const String lessonPath = '/learning/:courseId/:lessonId';
  static const String chatListPath = '/chat';
  static const String chatPath = '/chat/:conversationId';
  // ... 其他路径
}
```

### 4.4 router/app_router.dart

#### `goRouterProvider` (Provider<GoRouter>)

全局路由配置，包含：

1. **redirect 逻辑（onboarding 引导）**：
   - 读取 `SharedPreferences.onboarding_completed`
   - 未完成引导：除 `/onboarding`、`/onboarding/api-setup`、`/settings/api` 外全部重定向到 `/onboarding`
   - 已完成但访问引导页：重定向回 `/home`

2. **路由树结构**：
   - 顶层路由：`/onboarding`、`/onboarding/api-setup`
   - `ShellRoute`（导航壳）：包裹所有主导航页面
     - `/home`、`/learning`（含子路由 `:courseId/:lessonId`）、`/chat`（含 `:conversationId`）、`/notes`（含 `:noteId`）、`/achievements`、`/statistics`、`/settings`、`/settings/api`、`/help`

3. **`_AppShell`（导航壳 Widget）**：
   - 桌面端（≥1024px）：`NavigationRail` + `VerticalDivider`
   - 移动端：`NavigationBar`
   - 6 个主导航项：首页 / 学习 / 对话 / 笔记 / 成就 / 设置

### 4.5 theme/

`lib/core/theme/` 由 5 个文件协同构成完整的双主题（light/dark）系统，所有视觉令牌（颜色 / 渐变 / 阴影 / 形状）均通过 `ThemeExtension` 注入 `ThemeData.extensions`，UI 层统一通过 `context.lingxiColors` / `context.lingxiGradients` / `context.lingxiElevations` 扩展获取，**禁止**硬编码颜色值。

| 文件 | 职责 |
|------|------|
| `app_theme.dart` | `AppTheme` 静态类，提供 `lightTheme` / `darkTheme`，种子色 `0xFF6750A4`（紫色调），Noto Sans SC + Quicksand 字体；`darkTheme` 中通过 `brightness` 三元切换注册 `LingxiColors.dark` / `LingxiGradients.dark` / `LingxiElevations.dark` 三组 ThemeExtension |
| `lingxi_colors.dart` | `LingxiColors extends ThemeExtension<LingxiColors>`，6 个语义色（mascotPrimary 星空紫 / streakFire 火焰红 / achievementGold 成就金 / misconceptionRed 误解红 / successGreen 成功绿 / infoBlue 信息蓝），提供 `light` / `dark` 双静态实例；`dark` 实例校准 streakFire（0xFFFF7043→0xFFFF8A65）与 achievementGold（0xFFFFE082→0xFFFFD54F）以满足 WCAG AA 对比度（≥ 4.5:1） |
| `lingxi_gradients.dart` | `LingxiGradients extends ThemeExtension<LingxiGradients>`，6 个语义渐变（mascotHero 吉祥物主光 / streakFire 火焰 / achievementGold 成就金 / primarySurface 主色面 / celebration 庆祝 / success 成功），提供 `light` / `dark` 双静态实例 |
| `lingxi_elevations.dart` | `LingxiElevations extends ThemeExtension<LingxiElevations>`，重构为 3 档语义阴影 `subtle` / `elevated` / `highlighted`（light/dark 双实例），同时保留 `level0`~`level4` 兼容旧调用；`LingxiCard.elevation` 参数映射 0→subtle / 1→elevated / 2→highlighted |
| `shape_variants.dart` | 形状变体定义 |

### 4.6 motion/

| 文件 | 职责 |
|------|------|
| `spring_motion.dart` | 6 档弹簧规格（microSpeed / fastSpeed / defaultSpeed / gentleSpeed / slowSpeed / bouncySpeed，v0.3.0 修复 fastSpeed settle time 151ms → 148ms ≤ 150ms）+ 7 个过渡组件（`springTransition`/`slideFadeTransition`/`pulseBreathing`/`scalePressFeedback`/`hoverLift`/`shimmerGlow`/`sparkleRing`），自动检测 `disableAnimations` 降级；提供 `entranceCurve` / `exitCurve` / `reduceMotionOf` / `resolveDuration` 公开 API |
| `page_transitions.dart` | `LingxiPageTransitions` 提供 `buildPage`/`buildModalPage`/`buildSlidePage` 三种页面过渡；v0.3.0 新增公开静态方法 `slideFadeTransitionBuilder`（FadeTransition + SlideTransition，begin `Offset(0.0, 0.05)` → `Offset.zero`，曲线 `SpringMotion.entranceCurve`），`buildPage` 直接复用 |
| `animation_utils.dart` | 动画工具函数 |

#### 4.6.1 Hero 共享元素动画（v0.3.0 新增）

吉祥物在首页 / 学习路径 / 对话页之间通过 Hero 共享元素动画实现视觉延续。

| 组件 / API | 位置 | 职责 |
|------------|------|------|
| `Hero(tag: 'mascot-hero')` | `lib/features/home/home_page.dart` / `lib/features/learning/learning_path_page.dart` / `lib/features/chat/chat_page.dart` | 三处吉祥物外层包裹同名 Hero，tag 统一为 `'mascot-hero'` |
| `mascotHeroFlightShuttleBuilder` | `lib/features/mascot/` | 自定义 `flightShuttleBuilder`，飞行过程使用 `SpringMotion.gentleSpeed` 曲线插值位置与缩放，保证双端过渡手感一致 |
| `reduceMotion` 降级 | `MediaQuery.disableAnimationsOf(context)` 为 true 时 | Hero 动画降级为即时切换，跳过飞行过程 |

**使用约束**：同屏不可出现两个相同 tag 的 Hero，否则抛 `MultipleHeroException`。三处页面均为单吉祥物场景，已通过静态审查。

#### 4.6.2 按压微交互（v0.3.0 新增）

通用组件统一接入按压弹性反馈，所有按压动画在 `SpringMotion.fastSpeed`（148ms）下完成。

| 组件 | 文件 | 按压效果 | 触发条件 |
|------|------|----------|----------|
| `LingxiButton` | `lib/shared/widgets/lingxi_button.dart` | `AnimatedScale` 0.96 + 阴影从 `subtle` 抬升至 `elevated` | `onPressed != null` 且按压时 |
| `LingxiCard` | `lib/shared/widgets/lingxi_card.dart` | `AnimatedScale` 0.99 + 阴影抬升 | 仅可点击卡片（`onTap != null`）触发，不可点击卡片无 `AnimatedScale` |
| `LingxiChip` | `lib/shared/widgets/lingxi_chip.dart` | 选中态 `AnimatedSwitcher` + scale 0.95 | 选中态切换时触发 |

**降级**：`reduceMotion` 为 true 时，按压动画跳过 `AnimatedScale`，仅保留阴影抬升的瞬时切换。

#### 4.6.3 GoRouter 页面过渡统一（v0.3.0 升级）

所有 14 个 `GoRoute`（onboarding / apiSetup / home / learning / lesson / chatList / chat / notes / noteEditor / achievements / statistics / settings / settingsApi / help）统一通过 `LingxiPageTransitions.buildPage` / `buildSlidePage` / `buildModalPage` 返回 `CustomTransitionPage`。

| 要素 | 实现 |
|------|------|
| 过渡 builder | `LingxiPageTransitions.slideFadeTransitionBuilder`：`FadeTransition` + `SlideTransition`，begin `Offset(0.0, 0.05)` → `Offset.zero` |
| 入场曲线 | `SpringMotion.entranceCurve` |
| 出场曲线 | `SpringMotion.exitCurve` |
| 时长常量 | `slowDuration` / `defaultDuration` / `gentleDuration`（取自 `SpringMotion` 6 档规格） |
| 双端一致性 | Android / Windows 共享同一套常量与曲线，无平台分支 |
| `reduceMotion` 降级 | `slideFadeTransitionBuilder` / `_buildSlideTransition` / `_buildModalTransition` 在 `MediaQuery.disableAnimationsOf` / `SpringMotion.reduceMotionOf` 为 true 时直接返回 `child`（即时切换） |

**ShellRoute 兼容**：导航壳内子页面过渡未破坏 `_AppShell` 的 NavigationRail / NavigationBar 切换。

#### 4.6.4 列表滚动优化（v0.3.0 新增）

| 文件 | 优化项 | 说明 |
|------|--------|------|
| `lib/features/chat/chat_list_page.dart` | `cacheExtent: 500` | 对话项高度不固定，未加 `itemExtent`，通过预渲染后续项避免滚动时即时 build 抖动 |
| `lib/features/notes/notes_page.dart` | `cacheExtent: 500` | 笔记项高度不固定，同上策略 |
| `lib/features/learning/learning_path_page.dart` | `cacheExtent: 500` + `RepaintBoundary` | `_CourseCard`（含 `FutureBuilder` + `AnimatedProgressBar` + `shimmerGlow` 复杂子树）外层包裹 `RepaintBoundary`，隔离滚动时的相邻项重绘 |

#### 4.6.5 PageView 手感（v0.3.0 新增）

`lib/features/learning/lesson_page.dart` 的知识点 `PageView` 升级：

| 要素 | 实现 |
|------|------|
| 滑动物理 | `BouncingScrollPhysics`（双端统一 iOS 风格回弹），替代默认的 `PageScrollPhysics` |
| `reduceMotion` 降级 | `NeverScrollableScrollPhysics` 禁用滑动手势，底部进度条行追加左右 `IconButton`（chevron_left / chevron_right，到达首末页时禁用），通过 `_pageController.previousPage` / `nextPage` 切换（150ms `easeInOut`） |
| `pageSnapping` | 保留默认（自定义 0.3 阈值需禁用默认 `pageSnapping` 并手动处理 `ScrollEndNotification`，复杂度高且易与默认手势冲突，spec 允许保留默认） |

---

## 五、数据层（data）模块

### 5.1 db/database.dart（核心数据库）

#### `LingxiDatabase`（继承 `_$LingxiDatabase`）

- **schemaVersion**: 3
- **DateTime 存储**: `storeDateTimeAsText: true`（ISO8601 文本，保留毫秒精度）
- **迁移策略**: `onUpgrade` 按版本号顺序执行（v1→v2 新增 LearnerProfiles，v2→v3 新增 LearningEvents）
- **外键约束**: `beforeOpen` 中执行 `PRAGMA foreign_keys = ON`

#### 表清单（共 10 张表）

| 表 | 用途 | 主键 |
|----|------|------|
| `Conversations` | 对话元信息（标题、模型、时间戳） | id (UUID) |
| `Messages` | 对话消息（user/assistant/system，含 tokens） | id (UUID) |
| `Notes` | 笔记（可关联 conversationId/courseId/lessonId） | id (UUID) |
| `Progress` | 学习进度（status: not_started/in_progress/completed，score） | id (UUID) |
| `ApiKeys` | API Key **元数据**（仅元数据，真实 Key 不入库） | providerType |
| `Settings` | 通用 KV 设置 | key |
| `Achievements` | 成就解锁记录 | id |
| `Streaks` | 连续学习天数（单行表） | dayCount |
| `LearnerProfiles` | 学习者画像（单行表，id='default'） | id |
| `LearningEvents` | 学习行为事件（lesson_start/quiz_attempt 等 7 类） | id (UUID) |

#### 工厂构造函数

```dart
factory LingxiDatabase.open() => LingxiDatabase(openConnection());
factory LingxiDatabase.forTesting(QueryExecutor e) => LingxiDatabase(e);
```

### 5.2 db/connection.dart

跨端数据库连接初始化（顶层函数）：

- `getDatabaseFile()`：返回 `getApplicationDocumentsDirectory()/lingxi_academy.db`
- `openConnection()`：返回 `LazyDatabase`，延迟打开
  - Android：使用 `sqflite`，旧版本（<7.0）自动调用 `applyWorkaroundToOpenSqlite3OnOldAndroidVersions`
  - 桌面（Windows）：依赖 `sqlite3_flutter_libs` FFI
  - 通过 `NativeDatabase.createInBackground` 在后台 isolate 打开，避免阻塞 UI
  - 生产环境 `logStatements: false`

### 5.3 models/

#### `provider_config.dart`

| 类型 | 说明 |
|------|------|
| `enum ProviderType` | 4 种服务商：`openaiCompatible`、`anthropic`、`gemini`、`ollama`，采用 enhanced enum 直接持有 `value`、`displayName`、`defaultBaseUrl`、`defaultModel` 属性 |
| `class ProviderConfig` | 不可变配置类，**apiKey 仅在内存中持有**，`toJson()` 跳过 apiKey（安全红线） |

**默认 baseUrl / model 映射**（由 `ProviderType` 枚举属性直接提供）：

| ProviderType | 默认 baseUrl | 默认 model |
|--------------|--------------|-----------|
| `openaiCompatible` | `https://api.openai.com/v1` | `gpt-4o-mini` |
| `anthropic` | `https://api.anthropic.com` | `claude-3-5-sonnet-20241022` |
| `gemini` | `https://generativelanguage.googleapis.com` | `gemini-1.5-flash` |
| `ollama` | `http://localhost:11434` | `llama3.2` |

#### `course_content.dart`

灵犀学院学习内容数据模型，层级结构 `Course → Module → Lesson → KnowledgePoint`：

| 类 | 说明 |
|----|------|
| `enum CourseLevel` | l0-l4 五个级别 |
| `enum QuizType` | singleChoice / multipleChoice / fillInBlank |
| `class VocabularyTerm` | 术语词汇 |
| `class QuizQuestion` | 测验题目 |
| `class KnowledgePoint` | 知识点（含核心解释、为什么重要、词汇、测验、苏格拉底种子问题、常见误解、难度、预计时长、前置依赖） |
| `class Lesson` | 课时 |
| `class Module` | 模块 |
| `class Course` | 课程 |

所有模型手动实现 `fromJson`/`toJson`，用于加载 `assets/courses/` 下的课程 JSON。

### 5.4 repositories/

每张表对应一个 Repository，封装业务语义化方法，**不直接暴露 `db.select/update`**。

#### ConversationRepository

| 方法 | 职责 |
|------|------|
| `getAllConversations({limit, offset})` | 按 updatedAt 倒序，支持分页 |
| `watchConversations()` | Stream 监听列表变化 |
| `createConversation(title, {provider, model})` | 插入并返回完整记录（`insertReturning`） |
| `deleteConversation(id)` | 事务中同时删除对话与关联消息 |
| `updateTitle(id, title)` | 更新标题并刷新 updatedAt |
| `count()` | 统计对话总数 |

#### MessageRepository

- `addMessage(conversationId, role, content, {tokens})`：插入消息
- `getMessages(conversationId)`：按时间正序返回

#### NoteRepository

| 方法 | 职责 |
|------|------|
| `getAllNotes({tagFilter, limit, offset})` | 按 updatedAt 倒序，支持标签过滤（LIKE 匹配） |
| `watchNotes()` | Stream 监听 |
| `createNote({title, content, tags, conversationId, courseId, lessonId})` | 显式设置 updatedAt |
| `updateNote(id, {title?, content?, tags?})` | 仅更新非 null 字段 |
| `deleteNote(id)` / `count()` | 删除与统计 |

#### ProgressRepository

状态机管理 `not_started → in_progress → completed`：

- `markInProgress(...)`：记录不存在则新建；已 completed 不回退
- `markCompleted(..., {score=1.0})`：标记完成并记录 score 与 completedAt
- `getCompletionRate(courseId)`：completed 数 / 总数

#### ProviderConfigRepository（**安全核心**）

分层存储策略：
- **非密钥配置**（baseUrl/model/temperature/maxTokens/enabled）→ `SharedPreferences`，键 `provider_config_<type>`，值 JSON 字符串
- **API Key** → `SecureStorageService` 加密存储，键 `api_key_<type>`

| 方法 | 职责 |
|------|------|
| `getAllProviders()` / `getProvider(type)` | 返回配置，从 SecureStorage 注入 apiKey |
| `saveProvider(config)` | JSON 序列化非密钥部分；apiKey 单独写入 SecureStorage |
| `deleteProvider(type)` | 同时清理 SharedPreferences 与 SecureStorage |
| `getDefaultProvider()` | 返回首个 `enabled && apiKey.isNotEmpty` 的服务商 |

#### LearnerProfileRepository

单行表模式（id='default'）：
- `getProfile()`：返回当前画像，不存在时插入默认画像
- 单字段更新方法：`updateAgeGroup`、`updateSkillLevel`、`updateLearningGoal`、`updateDailyMinutes`、`updatePace`
- `updateProfile({...})`：批量更新

#### LearningEventRepository

记录学习行为事件：

| 方法 | 职责 |
|------|------|
| `recordEvent({eventType, courseId?, ...})` | 插入事件，metadata 用 `jsonEncode` |
| `getLastCompletionEvent(kpId)` | 查询知识点最近的完成/复习事件（用于遗忘曲线） |
| `getTodayEventCount()` | 今日事件总数 |
| `getEventsBetween(start, end)` | 时间范围查询 |
| `getSocraticTurnCount(courseId)` | 指定课程的苏格拉底对话总轮次 |

事件类型常量：`lessonStart` / `lessonComplete` / `quizAttempt` / `quizPass` / `socraticTurn` / `noteCreate` / `reviewComplete`

#### CourseRepository

从 `assets/courses/` 加载课程 JSON：
- `getAllCourses()`：按 `Course.order` 升序返回；通过 `AssetManifest.json` 动态发现课程文件清单；内存缓存 `List<Course>.unmodifiable`
- 单文件失败时跳过不影响其他课程
- `clearCache()`：清除缓存触发重新加载

#### AchievementRepository

- `getAllAchievements()` / `watchAchievements()`
- `unlockAchievement(id)`：置 `unlocked=true` 并记录 `unlockedAt`；已解锁时跳过保留原始解锁时间

### 5.5 services/secure_storage_service.dart

基于 `flutter_secure_storage` 封装 API Key 加密存储：

| 方法 | 职责 |
|------|------|
| `setApiKey(providerType, apiKey)` | 写入（键 `api_key_<providerType>`） |
| `getApiKey(providerType)` | 读取，未配置返回 null |
| `deleteApiKey(providerType)` | 删除单个 |
| `hasApiKey(providerType)` | 判断非空 |
| `deleteAllApiKeys()` | 先收集前缀键再删除，避免迭代时并发修改 |

各平台使用原生安全机制：Android Keystore / Windows DPAPI。

### 5.6 providers/

| 文件 | 职责 |
|------|------|
| `db_providers.dart` | `databaseProvider` 单例 + 各 Repository Provider 注册（依赖 `databaseProvider`） |
| `storage_providers.dart` | `secureStorageServiceProvider` + `providerConfigRepositoryProvider`（组合 SecureStorage + SharedPreferences） |
| `course_providers.dart` | `courseRepositoryProvider` + `allCoursesProvider`（FutureProvider 加载并缓存课程） |

---

## 六、功能层（features）模块

### 6.1 ai/ —— AI Provider 抽象与实现

#### `ai_provider.dart`（抽象接口）

定义所有 AI 服务商必须遵循的统一接口：

```dart
abstract class AiProvider {
  Stream<AiStreamEvent> chatStream({
    required List<ChatMessage> messages,
    required ChatOptions options,
  });
  Future<String> chat({...});
  void cancel();
  Future<bool> testConnection();
}
```

**关键数据模型**：

| 类型 | 说明 |
|------|------|
| `enum MessageRole` | system / user / assistant |
| `class ChatMessage` | 不可变值对象（role + content） |
| `class ChatOptions` | temperature / maxTokens / systemPrompt / stream |
| `sealed class AiStreamEvent` | 流式响应事件基类 |
| `TextDeltaEvent` | 内容增量 |
| `UsageEvent` | token 用量统计 |
| `DoneEvent` | 流式结束 |
| `ErrorEvent` | 流式错误 |

**契约**：流必须以 `DoneEvent` 或 `ErrorEvent` 终止；被 `cancel` 时静默结束不抛异常。

#### `ai_provider_registry.dart`

AI Provider 注册中心，有状态单例：

| 方法 | 职责 |
|------|------|
| `getProvider()` | 获取当前 Provider，缓存命中直接返回；否则从仓库读取默认配置创建 |
| `_createProvider(config)` | 根据 ProviderType switch 创建对应实现 |
| `switchProvider(type)` | 切换到指定类型 |
| `invalidateCache()` | 失效缓存（用户修改配置后调用） |
| `cancel()` | 取消当前进行中的请求 |

#### `ai_providers.dart`（Riverpod 注册）

| Provider | 类型 | 职责 |
|---------|------|------|
| `aiProviderRegistryProvider` | `Provider<AiProviderRegistry>` | 单例，依赖 `providerConfigRepositoryProvider` |
| `currentAiProviderProvider` | `FutureProvider<AiProvider?>` | 异步获取当前 Provider，可通过 `invalidate` 重建 |
| `promptManagerProvider` | `FutureProvider<PromptManager>` | 单例，首次监听时加载 assets 中的系统提示词 |

#### 四个具体 Provider 实现

| 文件 | 类 | 协议 | 鉴权 |
|------|----|----|------|
| `openai_compatible_provider.dart` | `OpenAICompatibleProvider` | POST `/chat/completions` + SSE | Bearer Token |
| `anthropic_provider.dart` | `AnthropicProvider` | POST `/v1/messages` + SSE | `x-api-key` 头 + `anthropic-version: 2023-06-01` |
| `gemini_provider.dart` | `GeminiProvider` | POST `/v1beta/models/{model}:streamGenerateContent?alt=sse` | URL 参数 `key={apiKey}` |
| `ollama_provider.dart` | `OllamaProvider` | POST `/api/chat` + NDJSON | 无需 API Key |

所有 Provider 共用：
- `dio` + `SecureLogInterceptor`（自动脱敏）
- `SseTransformer.parse()`（W3C SSE 解析，Ollama 用 NDJSON 按行解析）
- `AiErrorMapper.errorException()` 包装异常为 `ErrorEvent`

#### `sse_transformer.dart`

W3C SSE 规范解析器：
- `static parse(Stream<List<int>> byteStream) → Stream<String>`
- 按行分割 → `data:` 前缀提取 → 空行触发 emit → `:` 开头为注释跳过 → 剥离 UTF-8 BOM

#### `secure_log_interceptor.dart`（**安全核心**）

`SecureLogInterceptor extends Interceptor`，所有 dio 请求必经此拦截器：

| 脱敏类型 | 字段 |
|---------|------|
| 请求头 | `authorization`、`x-api-key`、`x-goog-api-key` → `[REDACTED]` |
| 查询参数 | `key`、`api_key` → `[REDACTED]` |
| 请求体字段 | `api_key`、`apikey`、`apiKey`、`key` → `[REDACTED]` |
| URL | 查询参数自动脱敏 |

#### `prompt_manager.dart`

提示词管理器，负责从 assets 加载苏格拉底式与直接解答两套系统提示词：

| 字段 | 说明 |
|------|------|
| `LearnerAgeGroup` 枚举 | `young`（10-15 岁）/ `advanced`（15+ 岁） |
| 加载文件 | `socratic_system_prompt.md` / `direct_answer_prompt.md` / `socratic_young_learner.md` / `socratic_advanced.md` |

| 方法 | 职责 |
|------|------|
| `loadPrompts()` | 预加载，失败时使用内置兜底提示词 |
| `getSystemPrompt({socraticMode, ageGroup})` | 根据模式与年龄段返回提示词 |
| `getSimplifyPrompt(originalAnswer)` | 生成"简化"分级探索提示词 |
| `getDeeperPrompt(originalAnswer)` | 生成"深入"分级探索提示词 |
| `getImagePrompt(originalAnswer)` | 生成"图示"分级探索提示词 |

#### 其他 AI 辅助类

| 文件 | 职责 |
|------|------|
| `ai_error_mapper.dart` | `AiErrorMapper.mapException()` 将异常映射为中文友好提示（401→鉴权失败、429→请求频繁等） |
| `confusion_detection_service.dart` | `ConfusionDetectionService` 检测用户困惑（关键词+极短回复），连续 3 次建议降级为直接解答 |
| `retry_interceptor.dart` | `RetryInterceptor` 网络错误自动重试（maxRetries=3，退避 500ms/1s/2s） |

### 6.2 chat/ —— 对话模块

#### `chat_controller.dart`（核心控制器）

`ChatController extends StateNotifier<ChatControllerState>`，管理消息收发、流式渲染、持久化与吉祥物联动。

**ChatControllerState 字段**：
- `messages`：已落盘的聊天消息
- `isStreaming`：是否正在接收流式响应
- `currentAssistantText`：当前流式响应累计文本（50ms 节流）
- `conversationId` / `conversationTitle`
- `currentTokens`：累计 token 数
- `error`：最近错误信息

**关键方法**：

| 方法 | 职责 |
|------|------|
| `sendMessage(text)` | 发送消息并触发流式响应（8 步流程） |
| `_onEvent(event)` | 处理 TextDeltaEvent/UsageEvent/DoneEvent/ErrorEvent |
| `_flush()` | 50ms 节流刷新缓冲区到状态 |
| `_commitAssistant()` | 完成流式：刷新文本、落盘助手消息、触发吉祥物庆祝 |
| `_finishStreamingWithError(message)` | 错误结束：保留已收到部分内容、吉祥物切难过 |
| `stopStreaming()` | 停止当前流式响应 |
| `saveAsNote(content)` | 将 AI 消息保存为笔记（标题取前 30 字，标签"对话"） |
| `loadConversation(id)` | 加载指定对话历史 |
| `reset()` | 重置为空白对话 |

**Provider**：`chatControllerProvider`（StateNotifierProvider，单例）

#### 其他 chat 文件

| 文件 | 类 | 职责 |
|------|----|------|
| `chat_page.dart` | `ChatPage` | 对话主页，含消息气泡、打字动画、流式脉冲、苏格拉底开关、Ctrl+Enter 快捷发送 |
| `chat_list_page.dart` | `ChatListPage` | 对话列表，Stream 监听，FAB 弹簧缩放入场 |
| `chat_desktop_layout.dart` | `ChatDesktopLayout` | 桌面双栏（左列表 320px + 右内容区） |

### 6.3 learning/ —— 学习路径与课时

#### `learning_path_page.dart`

`LearningPathPage extends ConsumerStatefulWidget`，展示 L0-L4 五层学习路径：
- 按 `CourseLevel` 分组渲染课程卡片
- 顶部 Chip 筛选 + 交错入场动画 + 连接线动画
- 课程进度条 + 完成打勾弹性动画
- 点击跳转 `${RouteNames.learningPath}/${course.id}/$lessonId`

#### `lesson_page.dart`

`LessonPage extends ConsumerStatefulWidget`，单节课学习页：
- `PageView` 轮播知识点
- 每知识点 5 阶段状态机：`学习卡片 → 测验 → 苏格拉底入口 → 苏格拉底对话 → 完成`
- 全部完成后显示章节庆祝页

**关键回调**：`_onKnowledgePointCompleted(kpId, score)` 持久化进度、触发吉祥物庆祝、纸屑粒子。

#### widgets/

| 文件 | 类 | 职责 |
|------|----|------|
| `quiz_widget.dart` | `QuizWidget` | 测验组件，支持单选/多选/填空，答对触发星光粒子，答错抖动+触觉反馈；正确率 ≥ `kQuizPassThreshold`(0.8) 视为通过 |
| `socratic_dialog_panel.dart` | `SocraticDialogPanel` | 测验通过后的苏格拉底对话面板，调用 `aiProvider.chatStream` 流式响应 |
| `learning_card_widget.dart` | `LearningCardWidget` | 知识点学习卡片 |
| `continue_learning_sidebar.dart` | `ContinueLearningSidebar` | 继续学习侧边栏 |

### 6.4 progress/ —— 进度统计与成就

#### `achievement_service.dart`

`AchievementService` 管理徽章解锁、进度更新：

| 方法 | 职责 |
|------|------|
| `_ensureSeeded()` | 幂等写入预定义徽章 |
| `getAll()` | 返回全部徽章及进度（VO: `AchievementWithProgress`） |
| `unlock(code)` | 解锁徽章并触发吉祥物 `celebrate()` |
| `updateProgress(code, progress)` | 更新进度，≥1.0 自动解锁 |
| `checkAndUnlockByEvent(event)` | 根据 `AchievementEvent` 类型分发判定 |
| `checkStreakAchievements(currentStreak)` | 检查 streak_7/streak_30 |

#### `streak_service.dart`

`StreakService` 连续学习天数计算：

| 方法 | 职责 |
|------|------|
| `getStreak()` | 读取当前 streak，无记录时插入初始行 |
| `recordStudyActivity()` | 根据 lastStudyDate 判断连续/断档/首次，事务保证原子更新 |
| `getCurrentStreakDays()` | 便捷返回当前连续天数 |

#### `statistics_page.dart`

`StatisticsPage` 统计页：
- `statisticsDataProvider`（autoDispose FutureProvider）并行加载 streak、notes、progress、socratic 计数
- 卡片：连续天数 / 苏格拉底对话数 / 笔记数 / 已完成知识点数
- `_BarChartPainter`：柱状图（本周/本月/全部）
- `_HeatmapPainter`：近 30 天学习热力图

#### `celebration_service.dart`

`CelebrationService`（单例）全局庆祝服务：
- 通过 `StreamController.broadcast` 广播 `CelebrationEvent`
- `GlobalCelebrationLayer` Widget 监听事件并在 Stack 上层显示粒子效果
- 便捷方法：`confetti(origin)` / `sparkles(...)` / `firework(...)`

#### `spaced_repetition_service.dart`

`SpacedRepetitionService` 基于简化的 Ebbinghaus 遗忘曲线（SM-2 变体）：
- 复习间隔：1/3/7/14/30 天
- `getTodayReviewList(courses)`：遍历知识点计算是否到复习窗口，按紧迫度排序

#### `achievement_definitions.dart`

定义 8 个预定义徽章：

| code | 名称 |
|------|------|
| `firstLesson` | 首课开学 |
| `firstQuizPass` | 测验首通 |
| `streak7` | 连续 7 天 |
| `streak30` | 连续 30 天 |
| `socratic100` | 苏格拉底百次对话 |
| `courseL0Complete` | L0 课程通关 |
| `notes50` | 笔记 50 篇 |
| `allLevelsExplore` | 全级别探索 |

`AchievementEvent` sealed 基类 + 5 个子事件类（LessonCompletedEvent / QuizPassedEvent / SocraticDialogEvent / NoteSavedEvent / LevelExploredEvent）。

### 6.5 recommendation/recommendation_service.dart

`RecommendationService` 基于规则的本地推荐引擎（不依赖 ML）：

| 方法 | 职责 |
|------|------|
| `getRecommendations(courses)` | 按完成率分支生成 4 类推荐（新课程/继续学习/复习），按优先级排序取前 5 |
| `getContinueLearningRecommendation(courses)` | 返回首个进行中课程的下一个未完成知识点（首页 CTA 用） |
| `_findNextIncompletePoint(course)` | 模块→课时→知识点三层查找首个未完成项 |
| `_getNewCoursePriority(level)` | L0=80 → L4=10，级别越低优先级越高 |

### 6.6 settings/

| 文件 | 类 | 职责 |
|------|----|------|
| `settings_page.dart` | `SettingsPage` | 配置中心主入口，分组渲染外观/语言/学习偏好/数据/API/关于 |
| `api_settings_page.dart` | `ApiSettingsPage` | AI 服务管理，列表项支持编辑/删除/激活切换 |
| `provider_edit_dialog.dart` | `ProviderEditDialog` | Provider 编辑表单，类型切换自动填充默认 baseUrl/model |
| `api_test_service.dart` | `ApiTestService` | 测试连接，sealed `ApiTestResult`（Success/AuthError/NetworkError/TimeoutError/UnknownError），`sanitize()` 脱敏 |
| `data_export_service.dart` | `DataExportService` / `DataImportService` | 数据导出/导入，事务化去重导入 |

**DataExportService 关键安全断言**：
```dart
assert(!json.containsKey('apiKey'));  // 不可移除
```

### 6.7 onboarding/

| 文件 | 类 | 职责 |
|------|----|------|
| `onboarding_page.dart` | `OnboardingPage` | 5 步引导（欢迎→自备 API→小犀彩蛋→学习路径→苏格拉底），桌面双列/移动单列 |
| `api_setup_wizard_page.dart` | `ApiSetupWizardPage` | 4 个 Provider 教程卡片，分步指导注册/获取 Key/填入/测试 |
| `learner_profile_setup_page.dart` | `LearnerProfileSetupPage` | 4 步问卷（ageGroup/skillLevel/dailyMinutes/pace），写入 LearnerProfiles 表 |

### 6.8 home/home_page.dart

`HomePage` 首页：
- `_recordStudyActivity()`：进入页面调用 `StreakService.recordActivity()`
- `_buildHero()`：问候区（按时间段切换"早安/午安/晚安"）
- `_buildContinueLearning()`：继续学习 CTA（基于 `RecommendationService`）
- `_buildQuickActions()`：快捷入口网格
- `_StreakBadge`：火焰渐变 + 流光

### 6.9 notes/

| 文件 | 类 | 职责 |
|------|----|------|
| `notes_page.dart` | `NotesPage` | 标签多选筛选，桌面网格/移动列表自适应 |
| `note_editor_page.dart` | `NoteEditorPage` | 新建/编辑双模式，`noteId='new'` 表示新建 |

### 6.10 help/help_center_page.dart

`HelpCenterPage` 帮助中心：
- 9 个分类卡片，可展开/收起（`AnimatedSize` + `AnimatedRotation`）
- Markdown 内容经 `MarkdownRenderer` 渲染
- FAB 触发反馈提交流程

### 6.11 mascot/ —— 吉祥物小犀

#### `mascot_state.dart`

```dart
enum MascotMood { idle, happy, thinking, sad, celebrate, curious }

class MascotState {
  final MascotMood mood;
  final bool isAnimating;
  final int tapCount;
}
```

**状态优先级**：sad > celebrate > thinking > happy > curious > idle

#### `mascot_controller.dart`

`MascotController extends StateNotifier<MascotState>`：

| 方法 | 职责 |
|------|------|
| `setMood(mood)` | 直接设定情绪，标记动画进行中 |
| `setAiThinking(thinking)` | 联动 AI 思考态 |
| `triggerTap()` | 2 秒内连点 5 次→celebrate 3s；否则 happy 1.5s |
| `celebrate()` | 触发庆祝动画 3s 后恢复 |
| `reset()` | 重置为初始待机 |

**Provider**：`mascotControllerProvider`（StateNotifierProvider，跨页面共享）

#### 其他 mascot 文件

| 文件 | 类 | 职责 |
|------|----|------|
| `mascot_widget.dart` | `MascotWidget` + `_MascotPainter`（内部） | 主 Widget，含光环 `_AuraGlow` 与气泡 `_SpeechBubble`，点击触发 `triggerTap()`，内部封装 `_MascotPainter` 作为 fallback 绘制 |
| `rive_mascot_widget.dart` | `RiveMascotWidget` | 目标方案，加载 `assets/rive/lingxi_mascot.riv`，加载失败降级为 `MascotWidget`（不再直接引用 `MascotPainter`） |
| `mascot_overlay.dart` | `MascotOverlay` | 全局浮层，mood != idle 时在右下角悬浮显示（72px 缩略版） |

#### `_MascotPainter` 视觉实现细节

`_MascotPainter`（`mascot_widget.dart` 内部私有类）作为 Rive 资源未就绪时的矢量 fallback 绘制器，已升级为具有完整视觉表现的实现：

| 视觉元素 | 实现 |
|---------|------|
| 身体填充 | 径向渐变（`LingxiColors.mascotPrimary` → 深紫 #5E35B1），营造立体感 |
| 角部高光 | 高光弧线点缀，强化"小犀"角色识别 |
| 眼睛 | 瞳孔高光点（白色小圆），增加灵性 |

6 种情绪差异化绘制：

| 情绪 | 视觉表现 |
|------|---------|
| `idle` | 眨眼动画 + 瞳孔高光点 |
| `happy` | 弧形笑眼 + 跳跃偏移 |
| `thinking` | 头顶问号浮动呼吸（缩放 + 透明度） |
| `sad` | 泪滴滑落动画（纵向位移 + 渐隐） |
| `celebrate` | 10 粒子 × 4 段拖尾辐射，圆周分布 |
| `curious` | 放大镜光泽弧线（旋转 + 透明度脉动） |

**`shouldRepaint` 优化**：仅在 `mood` / `animationValue` / `extraSparkle` 变化时返回 `true`，避免无谓重绘。

#### `_AuraGlow` 光环实现

`_AuraGlow`（`mascot_widget.dart` 内部私有 Widget）为吉祥物提供呼吸光环背景：

- 渐变：使用 `LingxiGradients.mascotHero` 渐变（随主题切换 light/dark 实例）
- 动画：4 秒呼吸脉动（scale 0.95 → 1.05），`Curves.easeInOut`
- 性能：`RepaintBoundary` 隔离，避免光环重绘触发吉祥物主体重绘

**动画时长标准**：

| 状态 | 持续时间 | 恢复目标 |
|------|----------|----------|
| happy | 1.5s | idle |
| celebrate | 3.0s | idle |
| thinking | 不定（跟随 AI 响应） | celebrate/sad |
| sad | 不自动恢复 | 等待下次交互 |
| curious | 2.0s | idle |

---

## 七、共享层（shared）模块

`lib/shared/` 不依赖任何 feature。

### 7.1 utils/

| 文件 | 类 | 职责 |
|------|----|------|
| `responsive.dart` | `Responsive` | 静态工具：`isMobile`(<600) / `isTablet`(600-1024) / `isDesktop`(≥1024) / `valueByDevice<T>` |
| `misconception_parser.dart` | `MisconceptionParser` | 从 AI 回复中提取 `[MISCONCEPTION]...[/MISCONCEPTION]` 标签，返回 `MisconceptionParseResult`（misconceptions + cleanText） |

### 7.2 widgets/

| 文件 | 类 | 职责 |
|------|----|------|
| `lingxi_card.dart` | `LingxiCard` | 统一卡片容器，4 种 variant（defaultColor/primary/secondary/glass），支持毛玻璃 |
| `lingxi_button.dart` | `LingxiButton` | 统一按钮，5 种 variant + 3 种 size，支持 `isLoading` 与 `enablePulse` 呼吸脉动 |
| `lingxi_app_bar.dart` | `LingxiAppBar` | AppBar，支持 mascot 头像、滚动联动透明度、staggered 入场 |
| `lingxi_text_field.dart` | `LingxiTextField` | 统一输入框 |
| `lingxi_dialog.dart` | `LingxiDialog` | 统一对话框 |
| `lingxi_chip.dart` | `LingxiChip` | Chip 组件 |
| `lingxi_badge.dart` | `LingxiBadge` | 徽章组件 |
| `lingxi_toast.dart` | `LingxiToast` | Toast 提示 |
| `markdown_renderer.dart` | `MarkdownRenderer` | Markdown 渲染，支持 LaTeX 数学公式（flutter_math_fork）+ 代码高亮 |
| `celebration_overlay.dart` | `CelebrationOverlay` | 命令式粒子爆发（confetti/sparkles/firework），含 `SuccessCheckmark` / `ErrorCross` |
| `animated_count_text.dart` | `AnimatedCountText` | 数字滚动动画 |
| `animated_progress_bar.dart` | `AnimatedProgressBar` | 进度条动画 |
| `empty_state_widget.dart` | `EmptyStateWidget` | 空状态占位 |
| `glass_container.dart` | `GlassContainer` | 毛玻璃容器 |
| `guide_bubble.dart` | `GuideBubble` | 引导气泡 |
| `level_exploration_buttons.dart` | `LevelExplorationButtons` | 分级探索按钮组（简化/深入/图示） |
| `shimmer_loading.dart` | `ShimmerLoading` | 骨架屏加载 |
| `misconception_sticker.dart` | `MisconceptionSticker` | 误解贴纸 |
| `particles/particle_painter.dart` | `ParticleSystem` / `ParticleEffect` | 粒子系统，支持 confetti/sparkles/firework 三种效果 |

---

## 八、关键业务流程

### 8.1 应用启动流程

```
main() 
  → WidgetsFlutterBinding.ensureInitialized()
  → SharedPreferences.getInstance()  // 异步初始化
  → ProviderScope(
      overrides: [sharedPreferencesProvider.overrideWithValue(prefs)],
      child: LingxiApp(),
    )
  → LingxiApp.build()
      → ref.watch(themeModeProvider) / localeProvider / goRouterProvider
      → MaterialApp.router(theme, darkTheme, routerConfig)
      → goRouterProvider.redirect: 检查 onboarding_completed
          → 未完成: 重定向 /onboarding
          → 已完成: 重定向 /home
```

### 8.2 AI 对话主链路

```
用户输入
  → ChatController.sendMessage(text)
      1. 校验 AI Provider 可用性（currentAiProviderProvider）
      2. 读取 ProviderConfig 获取 temperature/maxTokens
      3. 若对话不存在则创建 Conversation
      4. 持久化用户消息到 MessageRepository
      5. 联动吉祥物 setAiThinking(true) → thinking
      6. PromptManager.getSystemPrompt(socraticMode) 注入系统提示词
      7. provider.chatStream(messages, options) 发起流式请求
      8. 监听事件：
         - TextDeltaEvent → 缓冲 + 50ms 节流刷新 UI
         - UsageEvent → 累加 token
         - DoneEvent → _commitAssistant() 落盘 + 吉祥物 celebrate
         - ErrorEvent → _finishStreamingWithError() + 吉祥物 sad
```

### 8.3 单节课学习闭环

```
LearningPathPage 点击课程
  → context.go('/learning/:courseId/:lessonId')
  → LessonPage 加载课程数据
  → PageView 轮播知识点
      每个知识点状态机：
        1. 学习卡片（_KnowledgePointPageWrapper）
        2. 测验（QuizWidget，正确率 ≥ 0.8 通过）
        3. 苏格拉底入口
        4. 苏格拉底对话（SocraticDialogPanel，至少 1 轮）
        5. 完成
  → _onKnowledgePointCompleted(kpId, score)
      → ProgressRepository.markCompleted(...)
      → MascotController.celebrate()
      → CelebrationService.sparkles/confetti
      → 判断全部完成 → 章节庆祝页
```

### 8.4 API Key 安全链路（**安全核心**）

```
用户输入 API Key
  → ProviderEditDialog._save()
  → ProviderConfigRepository.saveProvider(config)
      → 非密钥配置 JSON → SharedPreferences（键 provider_config_<type>）
      → apiKey → SecureStorageService.setApiKey() 加密存储
  → AiProvider 构造时从 SecureStorage 读取 apiKey 传入内存
  → dio 请求经 SecureLogInterceptor 自动脱敏（authorization/x-api-key 等）
  → 数据导出 ProviderConfig.toJson() 跳过 apiKey
  → assert(!json.containsKey('apiKey'))  // 不可移除的断言
```

### 8.5 Onboarding 引导流程

```
首次启动
  → goRouterProvider.redirect 检测 onboarding_completed=false
  → 重定向 /onboarding
  → 5 步 PageView（欢迎→自备 API→小犀彩蛋→学习路径→苏格拉底）
      每步切换触发 mascotController.setMood(curious/happy)
  → _complete() 持久化 onboarding_completed=true
  → 重定向 /onboarding/api-setup
  → 4 个 Provider 教程卡片
  → 用户选择 Provider → 分步教程
  → _finish() 进入主页 /home
```

### 8.6 吉祥物状态机

```
各页面通过 ref.read(mascotControllerProvider.notifier).setMood(...)
  → MascotController 状态更新
  → MascotWidget / RiveMascotWidget 订阅渲染
  → MascotPainter 作为 fallback（纯矢量绘制）

状态联动：
  - AI 思考中 → setAiThinking(true) → thinking
  - AI 完成 → celebrate() → celebrate (3s) → idle
  - AI 出错 → setMood(sad)
  - 用户点击 → triggerTap()：单次 happy 1.5s；2s 内连点 5 次 → celebrate 3s

全局浮层：
  - MascotOverlay 监听 mood != idle → 右下角悬浮显示
```

### 8.7 数据持久化链路

```
UI → Repository（业务语义化方法）
   → LingxiDatabase（Drift）
   → openConnection()
       → Android: sqflite
       → 桌面: sqlite3_flutter_libs FFI
   → NativeDatabase.createInBackground（后台 isolate）
   → storeDateTimeAsText: true（ISO8601 存储）
```

---

## 九、依赖关系

### 9.1 核心依赖（dependencies）

| 依赖 | 版本 | 用途 |
|------|------|------|
| `flutter_riverpod` | ^2.5.1 | 状态管理（主框架） |
| `go_router` | ^14.2.0 | 声明式路由 |
| `drift` | ^2.18.0 | 类型安全 SQLite ORM |
| `sqlite3_flutter_libs` | ^0.5.24 | 桌面端 SQLite FFI |
| `path_provider` | ^2.1.3 | 跨端文件路径 |
| `path` | ^1.9.0 | 路径处理 |
| `sqflite` | ^2.3.3+1 | Android 端 SQLite |
| `uuid` | ^4.4.0 | UUID v4 主键生成 |
| `flutter_secure_storage` | ^9.2.2 | API Key 加密存储 |
| `dio` | ^5.4.3+1 | HTTP 客户端 |
| `rive` | ^0.13.13 | 吉祥物动画（目标方案） |
| `flutter_markdown` | ^0.7.2+1 | Markdown 渲染 |
| `flutter_math_fork` | ^0.7.2 | 数学公式渲染 |
| `markdown` | ^7.2.0 | Markdown 解析 |
| `google_fonts` | ^6.2.1 | 字体（Noto Sans SC + Quicksand） |
| `shared_preferences` | ^2.2.3 | KV 偏好存储 |
| `flutter_localizations` | sdk | 本地化委托 |

### 9.2 开发依赖（dev_dependencies）

| 依赖 | 版本 | 用途 |
|------|------|------|
| `flutter_lints` | ^4.0.0 | Lint 规则集 |
| `build_runner` | ^2.4.11 | 代码生成运行器 |
| `drift_dev` | ^2.18.0 | Drift 代码生成 |
| `json_serializable` | ^6.8.0 | JSON 序列化代码生成 |
| `flutter_launcher_icons` | ^0.13.1 | 应用图标生成 |
| `flutter_native_splash` | ^2.4.1 | 启动屏生成 |
| `http_mock_adapter` | ^0.6.1 | Dio HTTP mock（测试用） |
| `msix` | ^3.16.7 | Windows MSIX 打包 |

### 9.3 模块依赖方向图

```
┌─────────────────────────────────────────────────────────┐
│                    UI Layer (features/)                  │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ │
│  │ ai/  │ │chat/ │ │learn│ │home/ │ │prog/ │ │setng/│ │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ │
│     │        │        │        │        │        │     │
│     └────────┴────────┴────────┴────────┴────────┘     │
│                          │                              │
├──────────────────────────┼──────────────────────────────┤
│              State Layer (Riverpod Providers)            │
│                          │                              │
├──────────────────────────┼──────────────────────────────┤
│              Business Logic (Services)                   │
│  AchievementService / StreakService / CelebrationService │
│  SpacedRepetitionService / RecommendationService         │
│                          │                              │
├──────────────────────────┼──────────────────────────────┤
│             Data Access (Repositories)                   │
│  ConversationRepository / NoteRepository / ...           │
│                          │                              │
├──────────────────────────┼──────────────────────────────┤
│         Storage (Drift + SecureStorage)                  │
│  LingxiDatabase / SecureStorageService                   │
└──────────────────────────────────────────────────────────┘

跨层共享：
  - core/ (theme/router/constants/motion)  ← 被所有层依赖
  - shared/ (utils/widgets)               ← 被 features 层依赖，不依赖任何 feature
```

### 9.4 关键 Provider 依赖关系

```
sharedPreferencesProvider (main 中 override)
    ↓
appConfigProvider / themeModeProvider / localeProvider / socraticModeProvider
    ↓
secureStorageServiceProvider + sharedPreferencesProvider
    ↓
providerConfigRepositoryProvider
    ↓
aiProviderRegistryProvider → currentAiProviderProvider
    ↓
chatControllerProvider (依赖 currentAiProvider + promptManager + mascot + repos)
```

```
databaseProvider (LingxiDatabase.open)
    ↓
conversationRepositoryProvider / messageRepositoryProvider / noteRepositoryProvider
progressRepositoryProvider / settingsRepositoryProvider / achievementRepositoryProvider
learnerProfileRepositoryProvider / learningEventRepositoryProvider
    ↓
streakServiceProvider / achievementServiceProvider / spacedRepetitionServiceProvider
statisticsDataProvider / learningRecommendationsProvider
```

---

## 十、项目运行方式

### 10.1 环境要求

| 项 | 要求 |
|----|------|
| Flutter SDK | 3.44.4（推荐），兼容 `sdk: '>=3.10.0 <4.0.0'` |
| Dart SDK | 3.12.2（随 Flutter 3.44.4 自带） |
| 开发工具 | Trae / VS Code / Android Studio（需安装 Flutter 与 Dart 插件） |
| Android | minSdkVersion 24 |
| Windows | Windows 10+，需启用开发者模式 + Visual Studio C++ build tools |

### 10.2 Flutter SDK 路径

- **开发环境（本机）**：`C:\Users\23501\AppData\Local\Temp\flutter\bin\flutter.bat`
- **CI 环境**：直接使用 `flutter`（假定已加入 PATH）

### 10.3 从源码构建

```bash
# 1. 克隆仓库
git clone https://github.com/lingxiacademy/lingxi-academy.git
cd lingxi-academy

# 2. 安装依赖
flutter pub get

# 3. 运行代码生成（Drift / Riverpod / JSON）
flutter pub run build_runner build --delete-conflicting-outputs

# 4. 运行应用（开发模式）
flutter run

# 5. 构建发布版本
flutter build apk --release      # Android
flutter build windows --release  # Windows
```

### 10.4 各端运行命令

```bash
flutter run -d <android-device-id>  # Android 设备
flutter run -d windows               # Windows 桌面
flutter devices                      # 列出可用设备
```

### 10.5 常用命令速查

```bash
# 依赖管理
flutter pub get                                       # 安装依赖
flutter pub upgrade                                   # 升级依赖（谨慎）

# 代码生成
flutter pub run build_runner build --delete-conflicting-outputs   # 生成 .g.dart
flutter pub run build_runner watch --delete-conflicting-outputs   # 监听模式

# 静态分析
flutter analyze                                       # 必须零 error/warning

# 测试
flutter test                                          # 运行所有测试
flutter test test/data/                               # 运行指定目录
flutter test --coverage                               # 生成覆盖率

# 图标与启动屏
flutter pub run flutter_launcher_icons                # 生成应用图标
flutter pub run flutter_native_splash:create          # 生成启动屏
```

### 10.6 首次使用流程

1. **下载并安装应用**：从 [Releases 页面](https://github.com/lingxiacademy/lingxi-academy/releases) 下载对应平台安装包
2. **首次启动引导**：5 步引导页（欢迎 → 自备 API → 小犀彩蛋 → 学习路径 → 苏格拉底）
3. **配置 API**：进入 API 设置向导，选择服务商（OpenAI / Claude / Gemini / Ollama），填入 API Key 与模型名，点击"测试连接"验证
4. **开始学习或对话**：
   - 学习路径：首页 → 学习路径 → 选择 L0 课程开始
   - 自由对话：首页 → 对话 → 直接与 AI 交流

---

## 十一、安全红线与约束

> 🚨 **本章节为强制约束。任何修改若违反以下任一条，PR 将被直接拒绝。**

### 11.1 API Key 处理

- **API Key 只能通过 `SecureStorageService` 存储**（底层 `flutter_secure_storage`）
- **绝不**将 API Key 写入 Drift 数据库、`SharedPreferences`、文件、日志、导出 JSON
- `ApiKeys` 表只存元数据（providerType、baseUrl、model、temperature、maxTokens、enabled），**不含** apiKey 字段
- `ProviderConfig.apiKey` 字段仅在内存中持有，`toJson()` **必须**跳过 apiKey
- 构造 `AiProvider` 时从 `ProviderConfigRepository`（内部读 SecureStorage）获取 apiKey 传入

### 11.2 日志过滤

- **所有 dio 请求必须经过 `SecureLogInterceptor`**（`lib/features/ai/secure_log_interceptor.dart`）
- 自动脱敏字段：`authorization`、`x-api-key`、`x-goog-api-key`、`key`、`api_key`、`apiKey` 等 → 替换为 `[REDACTED]`
- **禁止**绕过 `SecureLogInterceptor` 直接 `print` 请求/响应内容
- 使用 `debugPrint` 而非 `print`（lint 规则 `avoid_print` 已启用）

### 11.3 数据导出

- `DataExportService.exportAll()` 中 `ProviderConfig.toJson()` **必须**不含 apiKey
- 代码中已有 `assert(!json.containsKey('apiKey'))` 断言，**不要移除**
- 导入时 `copyWith(apiKey: '')` 强制清空 apiKey
- 修改 `ProviderConfig.toJson` 时**必须**保持不含 apiKey 字段

### 11.4 .gitignore 必须包含敏感文件

```
*.keystore
*.jks
*.env
config.json
config.local.json
android/key.properties
macos/Runner/*.entitlements.priv
*.secure
```

### 11.5 截图/错误报告不能暴露 API Key

- 提交 Issue/PR 时，截图前确认无 API Key 残留
- 错误报告中若包含请求/响应，确保已通过 `SecureLogInterceptor` 脱敏
- **不要**在 Issue/PR 描述/commit message 中粘贴真实 API Key

### 11.6 不可修改的安全相关代码

除非任务明确要求，不要修改以下代码逻辑：
- `SecureStorageService`
- `SecureLogInterceptor` 的脱敏逻辑
- `ProviderConfig.toJson` 的字段过滤
- `DataExportService` 的 apiKey 断言
- `database.dart` 的 `storeDateTimeAsText`

---

## 十二、测试体系

### 12.1 测试目录结构

```
test/
├── core/
│   └── motion/
│       └── spring_motion_test.dart          # 6 档弹簧参数 + reduceMotion 降级（v0.3.0 新增，14 例）
├── data/
│   ├── repositories/                         # Repository 测试
│   │   ├── course_repository_test.dart
│   │   ├── provider_config_test.dart
│   │   └── repositories_test.dart
│   └── services/                             # 服务测试
│       └── secure_storage_test.dart
├── features/
│   ├── ai/                                   # AI Provider 测试
│   │   ├── confusion_detection_service_test.dart   # 困惑检测阈值与降级（v0.3.0 新增，18 例）
│   │   ├── prompt_manager_test.dart
│   │   ├── providers_test.dart
│   │   └── sse_transformer_test.dart
│   ├── learning/
│   │   └── course_level_extensions_test.dart # 级别色映射 L0-L4（v0.3.0 新增，16 例）
│   ├── mascot/
│   │   └── mascot_controller_test.dart       # 6 情绪切换 + 彩蛋 + mounted 检查（v0.3.0 新增，16 例）
│   ├── progress/
│   │   ├── spaced_repetition_service_test.dart  # 间隔重复算法（v0.3.0 新增，18 例）
│   │   └── streak_service_edge_test.dart        # 跨日边界 + 时区 + 补卡（v0.3.0 新增，15 例）
│   ├── recommendation/
│   │   └── recommendation_service_test.dart  # 推荐引擎（v0.3.0 新增，16 例）
│   └── settings/
│       └── api_test_service_test.dart
├── shared/
│   └── utils/
│       └── misconception_parser_test.dart
├── widget/                                   # Widget 测试（v0.3.0 新增目录）
│   ├── lingxi_button_test.dart               # 按压动画 + onPressed + 禁用态（8 例）
│   ├── lingxi_card_test.dart                 # 阴影层级 + 按压反馈（8 例）
│   └── mascot_widget_test.dart               # 6 情绪绘制差异 + 交互（20 例）
├── achievement_service_test.dart
├── chat_controller_test.dart
├── data_export_test.dart
├── empty_state_widget_test.dart
├── note_repository_test.dart
├── onboarding_page_test.dart
├── secure_log_interceptor_test.dart
├── socratic_mode_persistence_test.dart
├── streak_service_test.dart
└── widget_test.dart
```

#### 12.1.1 v0.3.0 新增测试文件清单

v0.3.0 共新增 10 个测试文件、149 个测试用例，覆盖吉祥物控制器 / Widget 渲染、业务逻辑边界（Streak 跨日 / 困惑检测 / 间隔重复 / 推荐引擎）、级别色映射、SpringMotion 参数与降级、通用组件按压反馈。

| 文件 | 用例数 | 覆盖范围 |
|------|--------|----------|
| `test/features/mascot/mascot_controller_test.dart` | 16 | setMood 6 情绪、triggerTap 单次/4 次/5 次彩蛋/1.5s 恢复/tapCount 持久性/happy 覆盖保护、setAiThinking、celebrate 即时/3s 恢复、mounted 检查、reset |
| `test/widget/mascot_widget_test.dart` | 20 | 6 情绪 CustomPaint 渲染、onTap 回调 + 连点 5 次、enableTapInteraction true/false、size 尺寸、showAura、speechBubble、mood 切换 |
| `test/features/progress/streak_service_edge_test.dart` | 15 | 跨日边界（23:59→00:01）、时区（UTC 日历字段）、断档与补卡、空状态 |
| `test/features/ai/confusion_detection_service_test.dart` | 18 | 困惑信号识别、计数与阈值（2 不降级 / 3 降级）、重置与降级、多对话隔离 |
| `test/features/progress/spaced_repetition_service_test.dart` | 18 | 复习队列触发（1/3/7/14/30 天窗口）、间隔重复紧迫度衰减、学习事件优先、多课程多知识点 |
| `test/features/recommendation/recommendation_service_test.dart` | 16 | 无历史 / 进行中 / 已完成分支、数量上限 5、优先级降序、getContinueLearningRecommendation |
| `test/features/learning/course_level_extensions_test.dart` | 16 | light/dark 双实例 L0-L4 语义色映射、5 级别互异、顺序一致、CourseLevel.fromValue 反查回退 |
| `test/core/motion/spring_motion_test.dart` | 14 | 6 档参数存在性、M3 规范、fastSpeed settle ≤ 150ms、单调递增、SpringSimulation 仿真、bouncySpeed 欠阻尼、entranceCurve/exitCurve、reduceMotionOf/resolveDuration 降级、kInstantDuration |
| `test/widget/lingxi_button_test.dart` | 8 | onPressed 回调、禁用态不响应、初始/按压/释放 scale 0.96、初始/按压阴影抬升、label/icon 渲染 |
| `test/widget/lingxi_card_test.dart` | 8 | elevation 0/1/2 阴影层级、默认 elevation=1、可点击 AnimatedScale、按压 scale 0.99、onTap 回调、child 渲染、默认 padding |
| **合计** | **149** | — |

### 12.2 测试约定

- 文件名：`xxx_test.dart`（与被测文件同名加 `_test` 后缀）
- 单元测试：`test('描述', () { ... })`
- Widget 测试：`testWidgets('描述', (tester) async { ... })`

### 12.3 内存数据库测试

```dart
final db = LingxiDatabase.forTesting(NativeDatabase.memory());
// 测试结束：await db.close();
```

- Repository 测试统一用 `NativeDatabase.memory()`，避免污染真实数据库
- SecureStorage 测试用 mock（见 `test/data/services/secure_storage_test.dart`）
- AI Provider 测试用 `http_mock_adapter` mock dio 请求

### 12.4 测试覆盖要求

- **Repository 必须有测试**（每个 Repository 至少覆盖 CRUD 主路径）
- **Service 必须有测试**（如 `StreakService`、`DataExportService`、`SecureLogInterceptor`）
- **AI Provider 必须有测试**（用 `http_mock_adapter` mock dio 请求）
- 新增功能时**必须**同步添加测试

### 12.5 性能预算

| 指标 | 目标 |
|------|------|
| 首屏渲染 | <1.5s |
| 流式首 token | <500ms（网络延迟除外） |
| 内存占用 | <200MB |
| 列表滚动 | 60fps |
| 动画帧率 | 60fps |
| 数据库查询 | <50ms |
| 课程加载 | <200ms |

**性能优化策略**：
- 流式响应使用 50ms 节流，避免频繁 setState
- 课程数据内存缓存，避免重复 IO
- 长列表使用 `ListView.builder` 懒加载
- 减少动效模式（`MediaQuery.disableAnimations`）时跳过所有动画

---

## 附录：关键文件速查表

| 文件路径 | 核心类/函数 | 一句话职责 |
|---------|------------|----------|
| `lib/main.dart` | `main()` | 应用入口，初始化 SharedPreferences |
| `lib/app.dart` | `LingxiApp` | MaterialApp.router 根 Widget |
| `lib/core/router/app_router.dart` | `goRouterProvider` / `_AppShell` | 路由配置 + 导航壳 |
| `lib/core/router/route_names.dart` | `RouteNames` | 路由常量 |
| `lib/core/providers/app_providers.dart` | `sharedPreferencesProvider` / `themeModeProvider` 等 | 全局 Provider |
| `lib/core/theme/app_theme.dart` | `AppTheme` | 主题定义 |
| `lib/core/constants/app_constants.dart` | `kAppName` / `kQuizPassThreshold` 等 | 全局常量 |
| `lib/core/motion/spring_motion.dart` | `SpringMotion` | 弹簧动画工具 |
| `lib/data/db/database.dart` | `LingxiDatabase` + 10 张表 | 数据库定义 |
| `lib/data/db/connection.dart` | `openConnection()` | 跨端数据库连接 |
| `lib/data/models/provider_config.dart` | `ProviderType`（enhanced enum，含 `defaultBaseUrl`/`defaultModel`）/ `ProviderConfig` | AI 配置模型 |
| `lib/data/models/course_content.dart` | `Course` / `KnowledgePoint` 等 | 课程内容模型 |
| `lib/data/repositories/provider_config_repository.dart` | `ProviderConfigRepository` | Provider 配置仓库（分层存储） |
| `lib/data/services/secure_storage_service.dart` | `SecureStorageService` | API Key 加密存储 |
| `lib/features/ai/ai_provider.dart` | `AiProvider` / `AiStreamEvent` | AI 抽象接口 |
| `lib/features/ai/ai_provider_registry.dart` | `AiProviderRegistry` | Provider 注册中心 |
| `lib/features/ai/openai_compatible_provider.dart` | `OpenAICompatibleProvider` | OpenAI 兼容实现 |
| `lib/features/ai/secure_log_interceptor.dart` | `SecureLogInterceptor` | 日志脱敏拦截器 |
| `lib/features/ai/sse_transformer.dart` | `SseTransformer.parse()` | SSE 流解析 |
| `lib/features/ai/prompt_manager.dart` | `PromptManager` | 提示词管理 |
| `lib/features/chat/chat_controller.dart` | `ChatController` / `chatControllerProvider` | 对话控制器 |
| `lib/features/learning/lesson_page.dart` | `LessonPage` | 课时学习页 |
| `lib/features/learning/widgets/quiz_widget.dart` | `QuizWidget` | 测验组件 |
| `lib/features/mascot/mascot_controller.dart` | `MascotController` / `mascotControllerProvider` | 吉祥物控制器 |
| `lib/features/mascot/mascot_state.dart` | `MascotMood` / `MascotState` | 吉祥物状态 |
| `lib/features/mascot/mascot_widget.dart` | `MascotWidget` + `_MascotPainter`（内部） | 吉祥物主入口，含 fallback 绘制 |
| `lib/features/progress/achievement_service.dart` | `AchievementService` | 成就服务 |
| `lib/features/progress/streak_service.dart` | `StreakService` | 连续学习服务 |
| `lib/features/settings/data_export_service.dart` | `DataExportService` | 数据导出 |
| `lib/shared/widgets/lingxi_card.dart` | `LingxiCard` | 统一卡片 |
| `lib/shared/widgets/lingxi_button.dart` | `LingxiButton` | 统一按钮 |
| `lib/shared/widgets/markdown_renderer.dart` | `MarkdownRenderer` | Markdown 渲染 |

---

*本文档基于项目当前状态生成，随项目演进持续更新。如有疑问，参考 [AGENTS.md](../AGENTS.md) 或 [docs/架构设计.md](架构设计.md) 获取更详细信息。*
